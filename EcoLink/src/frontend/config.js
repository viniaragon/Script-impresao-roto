// ============================================================
// EcoLink - Frontend Config
// Change API_URL to point to your Zeabur backend when deploying.
// Locally: leave as "" to use relative paths (/api/...)
// Online: set to your Zeabur URL, e.g. "https://ecolink-api.zeabur.app"
// ============================================================

const ECOLINK_CONFIG = {
    // Backend URL (empty = same origin, for local dev)
    API_URL: "",
};
