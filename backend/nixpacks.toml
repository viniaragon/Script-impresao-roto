# Configuração Nixpacks para Railway com Bun
# Desabilita detecção automática e força uso de Bun

providers = []

[phases.setup]
nixPkgs = ["bun"]

[phases.install]  
cmds = ["bun install"]

[start]
cmd = "bun run src/index.js"
